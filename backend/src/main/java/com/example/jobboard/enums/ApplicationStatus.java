package com.example.jobboard.enums;

/**
 * Tracks the lifecycle status of a job application.
 */
public enum ApplicationStatus {
    APPLIED,
    SHORTLISTED,
    REJECTED,
    HIRED,
    WITHDRAWN
}
